const NotFoundError = () => {
	return (
		<main className="not-found">
			<h1>Meal Not Found</h1>
			<p>Unfortunately, we could not find the requested meal data or page</p>
		</main>
	);
};
export default NotFoundError;
